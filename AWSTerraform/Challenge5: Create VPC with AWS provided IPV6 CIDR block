Create VPC with AWS provided IPV6 CIDR block
Note: Even if u want to create IPV6 only VPC, you need to give cidr_block parameter with some value (minimal range) that meets AWS requirements

resource "aws_vpc" "datacenter-vpc" {
    cidr_block = "10.0.0.0/16"
    assign_generated_ipv6_cidr_block = true
    tags = {
        Name = "datacenter-vpc"
    }
}
--------------------
provider.tf
terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "5.91.0"
    }
  }
}

provider "aws" {
  region  = "us-east-1"
}
